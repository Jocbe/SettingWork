{namespace view}

/**
 * Displays a full page with a given questionset
 * @param id
 * @param name
 * @param questions
 */
{template .set}
	{call shared.header /}
	{call shared.set.single data="all" /}
	<p><a href="/set/">Back to set list</a></p>
	{call shared.footer /}
{/template}