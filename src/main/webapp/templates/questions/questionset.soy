{namespace questions}

/**
 * Template of a page with all question sets
 * @param sets
 */
{template .allquestionsets}
	{call shared.header}
		{param title: 'Set list' /}
	{/call}
	<h1>All sets</h1>
	{call shared.set.multiple data="all" /}
	{call shared.footer /}
{/template}